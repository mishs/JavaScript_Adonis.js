@layout('main')
//at layout above extends layout from 'main' in view

@section('content')
    <h1> {{ title }}</h1>
    <ul>
        @each(post in posts)
        <li>{{ post.title }}</li>
        @endeach
    </ul>
@endsection